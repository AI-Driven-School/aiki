# ハンズオンチュートリアル - TODOアプリを作る

このチュートリアルでは、3AI協調開発テンプレートを使って**TODOアプリ**を0から本番デプロイまで完成させます。

所要時間: 約30分

---

## 目次

1. [プロジェクト作成](#1-プロジェクト作成)
2. [Phase 1: 要件定義](#phase-1-要件定義)
3. [Phase 2: 画面設計](#phase-2-画面設計)
4. [Phase 3: API設計](#phase-3-api設計)
5. [Phase 4: DB設計](#phase-4-db設計)
6. [Phase 5-7: 実装・テスト・レビュー（自動）](#phase-5-7-実装テストレビュー)
7. [Phase 8: デプロイ](#phase-8-デプロイ)
8. [完成](#完成)

---

## 1. プロジェクト作成

```bash
# テンプレートをインストール
curl -fsSL https://raw.githubusercontent.com/yu010101/claude-codex-collab/main/install-fullstack.sh | bash -s -- todo-app

# ディレクトリに移動
cd todo-app

# Claude Code起動
claude
```

起動後、以下のメッセージが表示されます:

```
┌─────────────────────────────────────────────────────────────┐
│   3AI協調開発テンプレート v5.0                              │
│   要件定義 → 設計 → 実装 → テスト → デプロイ               │
└─────────────────────────────────────────────────────────────┘
```

---

## Phase 1: 要件定義

### コマンド実行

```
> /project TODOアプリ
```

### Claudeの出力

```
[1/8] 要件定義を生成中...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 docs/requirements/todo-app.md を生成しました
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 生成される要件定義書

```markdown
# 要件定義: TODOアプリ

## ユーザーストーリー

AS A 一般ユーザー
I WANT TO タスクを作成・管理したい
SO THAT 日々のタスクを効率的に整理できる

## 受入条件

### 基本機能
- [ ] タスクを新規作成できる
- [ ] タスク一覧を表示できる
- [ ] タスクを完了状態に変更できる
- [ ] タスクを削除できる
- [ ] タスクを編集できる

### UI/UX
- [ ] レスポンシブデザイン（モバイル対応）
- [ ] キーボードショートカット（Enter で追加）
- [ ] 完了タスクは取り消し線で表示

### データ
- [ ] タスクはブラウザリロード後も保持される
- [ ] 作成日時が記録される

## 非機能要件

- パフォーマンス: 初回ロード 2秒以内
- アクセシビリティ: WCAG 2.1 AA準拠
- ブラウザ: Chrome, Safari, Firefox, Edge 最新版

## 制約事項

- フレームワーク: Next.js 14 (App Router)
- スタイリング: Tailwind CSS
- 状態管理: React hooks
- データ永続化: localStorage（Phase 1）
```

### 承認

```
承認しますか？ [Y/n/reject 理由]
> Y

✓ 要件定義を承認しました
```

---

## Phase 2: 画面設計

### Claudeの出力

```
[2/8] 画面設計を生成中...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 docs/specs/todo-list.md を生成しました
🖼️ mockups/todo-list.png を生成しました
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 生成されるモックアップ

```
┌──────────────────────────────────────────────────────────────┐
│  ☐ TODO                                              [+]    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 新しいタスクを入力...                           [追加] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ○ 牛乳を買う                            2024/01/15    │ │
│  │                                         [編集] [削除] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ● ━━レポートを提出━━                      2024/01/14    │ │
│  │                                         [編集] [削除] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ○ 会議の準備                            2024/01/15    │ │
│  │                                         [編集] [削除] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ──────────────────────────────────────────────────────────  │
│  全3件 | 完了: 1件 | 残り: 2件                               │
└──────────────────────────────────────────────────────────────┘
```

### 生成される画面設計書

```markdown
# 画面設計: TODOリスト

## 概要

タスクの一覧表示・追加・編集・削除を行うメイン画面。

## モックアップ

![TODOリスト](../mockups/todo-list.png)

## コンポーネント一覧

| コンポーネント | 種類 | 説明 |
|--------------|------|------|
| Header | Layout | アプリタイトルと追加ボタン |
| TaskInput | Form | 新規タスク入力フィールド |
| TaskList | List | タスク一覧コンテナ |
| TaskItem | ListItem | 個別タスク表示 |
| TaskCheckbox | Input | 完了状態トグル |
| TaskActions | ButtonGroup | 編集・削除ボタン |
| Footer | Layout | 統計情報表示 |

## 状態

| 状態名 | トリガー | 表示内容 |
|-------|---------|---------|
| 初期表示 | ページロード | 保存済みタスク一覧 |
| 空状態 | タスク0件 | 「タスクがありません」 |
| 追加中 | 入力→追加 | 楽観的UI更新 |
| 編集中 | 編集クリック | インライン編集モード |
| 削除確認 | 削除クリック | 確認ダイアログ |

## インタラクション

### キーボードショートカット

| キー | アクション |
|------|----------|
| Enter | タスク追加 |
| Escape | 編集キャンセル |

### アニメーション

- タスク追加: フェードイン + スライドダウン
- タスク完了: チェックマークアニメーション + 取り消し線
- タスク削除: フェードアウト + スライドアップ
```

### 承認

```
承認しますか？ [Y/n/reject 理由]
> Y

✓ 画面設計を承認しました
```

---

## Phase 3: API設計

### Claudeの出力

```
[3/8] API設計を生成中...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 docs/api/todo.yaml を生成しました
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 生成されるAPI設計（OpenAPI 3.0）

```yaml
openapi: 3.0.0
info:
  title: TODO API
  version: 1.0.0
  description: TODOアプリのREST API

servers:
  - url: /api/v1

paths:
  /todos:
    get:
      summary: タスク一覧取得
      operationId: getTodos
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Todo'

    post:
      summary: タスク新規作成
      operationId: createTodo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTodoRequest'
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Todo'

  /todos/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      summary: タスク詳細取得
      operationId: getTodo
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Todo'
        '404':
          description: 見つからない

    patch:
      summary: タスク更新
      operationId: updateTodo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTodoRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Todo'

    delete:
      summary: タスク削除
      operationId: deleteTodo
      responses:
        '204':
          description: 削除成功

components:
  schemas:
    Todo:
      type: object
      required:
        - id
        - title
        - completed
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
          maxLength: 200
        completed:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateTodoRequest:
      type: object
      required:
        - title
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200

    UpdateTodoRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 200
        completed:
          type: boolean
```

### 承認

```
承認しますか？ [Y/n/reject 理由]
> Y

✓ API設計を承認しました
```

---

## Phase 4: DB設計

### Claudeの出力

```
[4/8] DB設計を生成中...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 migrations/001_todos.sql を生成しました
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 生成されるマイグレーション

```sql
-- Migration: todos
-- Created: 2024-01-15

-- ===== テーブル作成 =====
CREATE TABLE todos (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title VARCHAR(200) NOT NULL,
    completed BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- ===== インデックス =====
CREATE INDEX idx_todos_completed ON todos(completed);
CREATE INDEX idx_todos_created_at ON todos(created_at DESC);

-- ===== トリガー（updated_at自動更新）=====
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ language 'plpgsql';

CREATE TRIGGER update_todos_updated_at
    BEFORE UPDATE ON todos
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

-- ===== コメント =====
COMMENT ON TABLE todos IS 'TODOタスク';
COMMENT ON COLUMN todos.id IS 'タスクID（UUID）';
COMMENT ON COLUMN todos.title IS 'タスクタイトル（最大200文字）';
COMMENT ON COLUMN todos.completed IS '完了フラグ';
COMMENT ON COLUMN todos.created_at IS '作成日時';
COMMENT ON COLUMN todos.updated_at IS '更新日時';
```

### 承認

```
承認しますか？ [Y/n/reject 理由]
> Y

✓ DB設計を承認しました

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 設計フェーズ完了！
  実装フェーズに進みます（自動実行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Phase 5-7: 実装・テスト・レビュー

設計承認後、以下が**自動実行**されます:

```
[5/8] 実装中...
      → src/app/page.tsx
      → src/components/todo/TaskInput.tsx
      → src/components/todo/TaskList.tsx
      → src/components/todo/TaskItem.tsx
      → src/lib/api/todos.ts
      → src/types/todo.ts

[6/8] テスト生成中...（Codex）
      → tests/todo.spec.ts

[7/8] レビュー中...（Codex）
      → docs/reviews/20240115-todo-app.md
```

### 生成されるコード例

#### src/components/todo/TaskItem.tsx

```tsx
'use client';

import { useState } from 'react';
import { Todo } from '@/types/todo';

interface TaskItemProps {
  todo: Todo;
  onToggle: (id: string) => void;
  onDelete: (id: string) => void;
  onUpdate: (id: string, title: string) => void;
}

export function TaskItem({ todo, onToggle, onDelete, onUpdate }: TaskItemProps) {
  const [isEditing, setIsEditing] = useState(false);
  const [editTitle, setEditTitle] = useState(todo.title);

  const handleSubmit = () => {
    if (editTitle.trim()) {
      onUpdate(todo.id, editTitle.trim());
      setIsEditing(false);
    }
  };

  return (
    <div className={`
      group flex items-center gap-3 p-4 bg-white rounded-lg border
      transition-all duration-200
      ${todo.completed ? 'opacity-60' : 'hover:shadow-md'}
    `}>
      {/* チェックボックス */}
      <button
        onClick={() => onToggle(todo.id)}
        className={`
          w-6 h-6 rounded-full border-2 flex items-center justify-center
          transition-colors duration-200
          ${todo.completed
            ? 'bg-green-500 border-green-500 text-white'
            : 'border-gray-300 hover:border-green-500'
          }
        `}
      >
        {todo.completed && (
          <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
          </svg>
        )}
      </button>

      {/* タイトル */}
      {isEditing ? (
        <input
          type="text"
          value={editTitle}
          onChange={(e) => setEditTitle(e.target.value)}
          onBlur={handleSubmit}
          onKeyDown={(e) => e.key === 'Enter' && handleSubmit()}
          className="flex-1 px-2 py-1 border rounded"
          autoFocus
        />
      ) : (
        <span className={`flex-1 ${todo.completed ? 'line-through text-gray-400' : ''}`}>
          {todo.title}
        </span>
      )}

      {/* 日付 */}
      <span className="text-sm text-gray-400">
        {new Date(todo.createdAt).toLocaleDateString('ja-JP')}
      </span>

      {/* アクション */}
      <div className="opacity-0 group-hover:opacity-100 flex gap-2 transition-opacity">
        <button
          onClick={() => setIsEditing(true)}
          className="p-1 text-gray-400 hover:text-blue-500"
        >
          編集
        </button>
        <button
          onClick={() => onDelete(todo.id)}
          className="p-1 text-gray-400 hover:text-red-500"
        >
          削除
        </button>
      </div>
    </div>
  );
}
```

### 生成されるレビュー結果

```markdown
# コードレビュー: TODOアプリ

## サマリー

- ✅ 受入条件: 10/10 クリア
- ✅ テストカバレッジ: 85%
- ⚠️ 軽微な改善提案: 2件

## 改善提案

### 1. アクセシビリティ

```tsx
// Before
<button onClick={() => onDelete(todo.id)}>削除</button>

// After
<button
  onClick={() => onDelete(todo.id)}
  aria-label={`${todo.title}を削除`}
>
  削除
</button>
```

### 2. パフォーマンス

```tsx
// メモ化を推奨
const TaskItem = memo(function TaskItem({ ... }) {
  // ...
});
```

## テスト結果

```
✓ タスクを追加できる (52ms)
✓ タスクを完了状態に変更できる (28ms)
✓ タスクを削除できる (31ms)
✓ タスクを編集できる (45ms)
✓ 空のタスクは追加できない (12ms)
✓ リロード後もタスクが保持される (89ms)

Tests: 6 passed
Time:  1.23s
```
```

---

## Phase 8: デプロイ

```
[8/8] デプロイ準備完了

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

レビュー結果:
  ✅ 受入条件クリア
  ✅ テストパス
  ⚠️ 軽微な改善提案あり（デプロイに影響なし）

本番にデプロイしますか？ [Y/n]
> Y

デプロイ中...
  → Vercelにデプロイ

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ デプロイ完了！

  URL: https://todo-app-abc123.vercel.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 完成

### 成果物一覧

```
todo-app/
├── docs/
│   ├── requirements/
│   │   └── todo-app.md          # 要件定義書
│   ├── specs/
│   │   └── todo-list.md         # 画面設計書
│   ├── api/
│   │   └── todo.yaml            # API設計（OpenAPI）
│   └── reviews/
│       └── 20240115-todo-app.md # レビュー記録
├── mockups/
│   └── todo-list.png            # モックアップ画像
├── migrations/
│   └── 001_todos.sql            # DBマイグレーション
├── src/
│   ├── app/
│   │   └── page.tsx
│   ├── components/
│   │   └── todo/
│   │       ├── TaskInput.tsx
│   │       ├── TaskList.tsx
│   │       └── TaskItem.tsx
│   ├── lib/
│   │   └── api/
│   │       └── todos.ts
│   └── types/
│       └── todo.ts
└── tests/
    └── todo.spec.ts
```

### 確認コマンド

```
> /status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
プロジェクト: TODOアプリ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[1/8] 要件定義    ✅ 承認済み   docs/requirements/todo-app.md
[2/8] 画面設計    ✅ 承認済み   docs/specs/todo-list.md
[3/8] API設計     ✅ 承認済み   docs/api/todo.yaml
[4/8] DB設計      ✅ 承認済み   migrations/001_todos.sql
[5/8] 実装        ✅ 完了       src/
[6/8] テスト      ✅ 完了       tests/
[7/8] レビュー    ✅ 完了       docs/reviews/
[8/8] デプロイ    ✅ 完了       https://todo-app-abc123.vercel.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 次のステップ

### 機能追加

```
> /project タスクのカテゴリ機能
```

### 個別コマンドで拡張

```
> /requirements ユーザー認証
> /spec ログイン画面
> /mockup ログイン画面
```

---

## Tips

### 承認を却下して再生成

```
承認しますか？ [Y/n/reject 理由]
> reject もっとシンプルな設計にしてほしい

再生成中...
```

### フェーズをスキップ

```
承認しますか？ [Y/n/reject 理由]
> skip

⚠️ 画面設計をスキップしました
   （後で /spec で個別生成できます）
```

### 進行中プロジェクトを再開

```
> /project --resume

進行中のプロジェクト:
  1. TODOアプリ (Phase 4: DB設計)
  2. ユーザー認証 (Phase 2: 画面設計)

どれを再開しますか？
> 1
```
